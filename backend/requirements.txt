# Runtime dependencies for the Slashit backend.
#
# THIS FILE IS WHAT SHIPS. The Dockerfile installs it and nothing else, so every
# line here is weight in the deployed image. Keep it to what application code
# actually imports.
#
# The LangChain and machine-learning toolkit lives in `requirements-ai.txt`,
# which is installed locally and never by the image. A package graduates from
# there to here when code imports it, with the reason in the dev log.
#
# Versions resolved from PyPI on 2026-09-12 and pinned. See 04.1 task T-1.1.
# Each slice appends; none rewrites.

# API and GraphQL
fastapi==0.141.1
uvicorn[standard]==0.52.4
strawberry-graphql[fastapi]==0.327.7

# Configuration
pydantic-settings==2.15.0

# Logging
structlog==26.1.0

# --- Added by slice 2 (identity and isolation), 2026-09-12 ---

# Database. Decisions AD-3, AD-5, AD-4 in the tech stack.
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.20.0

# Supabase JWT verification against the JWKS endpoint.
pyjwt[crypto]==2.14.0

# --- Added by slice 3 (the gateway), 2026-09-12 ---

# The model framework. Sits below the gateway's ModelProvider Protocol and is
# imported by app/domains/gateway/adapters/ and nowhere else.
# The full `langchain` package is deliberately NOT here: it carries agents,
# chains and memory that this gateway does not use. The wider AI toolkit lives
# in requirements-ai.txt and is never installed into the image.
langchain-core==1.6.3
langchain-google-genai==4.4.0

# --- Added by 002-authentication, slice 1 (04.1 task T-1.6), 2026-09-14 ---

# Background jobs. Rule T2 of the tech stack names Procrastinate; this is its
# first real use. PsycopgConnector needs psycopg[pool] and croniter, both
# pulled in as its own dependencies.
procrastinate==3.9.0

# --- Added by 002-authentication, slice 1, FR-17 moved into the app, 2026-09-19 ---

# SignInInteractor calls Supabase's Auth REST API directly (the Password
# Verification Attempt hook is Teams/Enterprise only). Already a transitive
# dependency of fastapi's test client; now imported by application code too.
httpx==0.28.1
