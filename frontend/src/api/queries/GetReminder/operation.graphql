#import "../../../fragments/ReminderFields.graphql"

query GetReminder($id: ID!) {
  reminder(id: $id) {
    __typename
    ... on Reminder {
      ...ReminderFields
    }
    ... on ReminderNotFound {
      message
    }
  }
}
