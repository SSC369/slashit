#import "../../../fragments/MemoryFields.graphql"

query GetMemory($id: ID!) {
  memory(id: $id) {
    __typename
    ... on Memory {
      ...MemoryFields
    }
    ... on MemoryNotFound {
      message
    }
  }
}
