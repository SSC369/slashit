---
doc: dev-log
feature: 002-authentication
title: Authentication
stage: 5
status: draft
owner: claude
created: 2026-09-14
updated: 2026-09-19
approved_on: null
supersedes: null
---

# Dev Log — Authentication

Context: [Implementation plan](./04-implementation-plan.md)

Appended as work happens. Records reality, including where reality left the
plan.

## Task status
Task ids come from the implementation plan. When it was split, the id carries
the sub-plan number and the sub-plan column names it.

| # | Sub-plan | Task | Status | Commit | Note |
|---|---|---|---|---|---|
| T-1.7 | 04.1-manual-signup-and-signin | `AuthCard`, `OtpInput` shared components | done | — | `frontend/src/features/auth/components/{AuthCard,OtpInput,styles}.ts(x)`. See deviation row below on the dot-grid colour. |
| T-1.1 | 04.1-manual-signup-and-signin | Migration `0007_profiles.py`: table, RLS, `handle_new_user()` trigger | done | — | Applied to the dev database and exercised live: username from `raw_user_meta_data`, `NULL` username with no collision, case-insensitive uniqueness rejects a duplicate. |
| T-1.2 | 04.1-manual-signup-and-signin | Migration `0008_auth_attempts.py`: table, RLS forced, `supabase_auth_admin`-only grant | done | — | Applied to the dev database; confirmed live via `information_schema.role_table_grants` that `authenticated`/`anon` hold no `SELECT`/`INSERT`/`UPDATE`/`DELETE` on the table. |
| T-1.3 | 04.1-manual-signup-and-signin | Migration `0009_auth_hooks.py`: both hook functions | done | — | Applied to the dev database and exercised live via raw SQL calls: 5th failed sign-in locks for 15 minutes and a 6th valid password is still rejected inside the lock; 6th signup in an hour from one email is rejected. Payload/response shapes are unverified against a real Supabase hook invocation — see deviation row. |
| T-1.5 | 04.1-manual-signup-and-signin | `profiles` repository, interactor, `me` GraphQL query | done | — | `Context` gained an `email` field, sourced from the JWT via a new `decode_email_claim` in `core/auth.py` rather than changing `verify_token`'s return type. Integration-tested end to end against the dev database, including the trigger-populated username. |
| T-1.6 | 04.1-manual-signup-and-signin | `purge_unverified_accounts` job | done | — | `core/jobs.py` (new) wires a `procrastinate.App`; `identity/jobs.py` (new) registers the hourly periodic task and runs it against a service-role session. Wired into `main.py`'s lifespan (`open_async`/`close_async`). See deviations for what this does not cover. |
| T-1.8 | 04.1-manual-signup-and-signin | `SignUpController` | done | — | `frontend/src/features/auth/controllers/SignUpController/{SignUpController.tsx,styles.ts}`. All states from `02-design.md` §4 (empty, loading, error/email-taken, error/username-taken, rate-limited, success). Calls `supabaseClient.auth.signUp` directly, no store or GraphQL operation, per the build plan's "browser talks to Supabase Auth directly". Google button renders the canvas's SVG verbatim with a no-op `handleGoogleSignIn` (comment points at 04.3-google-sign-in.md). `npm run lint` and `npm run build` both pass. See deviations for two guessed error-matching strings. |
| T-1.9 | 04.1-manual-signup-and-signin | `VerifyEmailController` | done | — | `frontend/src/features/auth/controllers/VerifyEmailController/{VerifyEmailController,styles}.ts(x)`. Reads `email` from the `?email=` query param, falling back to `location.state.email`, falling back to generic copy with no crash. Auto-submits `verifyOtp({type:"signup"})` on the 6th digit and via the explicit `Verify` button (disabled until 6 digits are entered). Resend calls `resend({type:"signup"})` unconditionally; a 30s client-side cooldown is UX only, Supabase's own limit is the real gate. Wrong-vs-expired is distinguished cleanly via `error.code === "otp_expired"` from `@supabase/auth-js`'s `ErrorCode` union — no ambiguity to log, contrary to what the task anticipated. Rate limiting is detected via `error.status === 429 \|\| error.code === "over_request_rate_limit"`. `lint`/`build` pass. |
| T-1.10 | 04.1-manual-signup-and-signin | `SignInController` | done | — | `frontend/src/features/auth/controllers/SignInController/{SignInController,styles}.ts(x)`. All six states (empty/loading/error/blocked/limited/success). Wrong-password vs. unregistered-email is a non-issue by construction: both fall through the same `setViewState("error")` branch, so the copy cannot diverge (FR-11). Unverified-account detection is solid: `error.code === "email_not_confirmed"`, a documented, distinct code in `@supabase/auth-js`'s `ErrorCode` union (separate from `invalid_credentials`). Lockout detection is a guess, same class as `SignUpController`'s and `VerifyEmailController`'s rate-limit matches — see deviation row. Google button renders the canvas's SVG verbatim with a no-op `handleGoogleSignIn` (comment points at 04.3-google-sign-in.md). `npm run lint` and `npm run build` both pass. |
| T-1.11 | 04.1-manual-signup-and-signin | `AuthStore`, `GetMe` operation folder, `AppShell` wiring | done | — | `frontend/schema.graphql` regenerated from the live backend schema (`strawberry.printer.print_schema`, the CLI's `export-schema` needs an uninstalled `libcst` extra) — it was stale, still had the placeholder `me: String!`. `src/api/queries/GetMe/{operation.graphql,operation.generated.ts,responseHandler.ts,useGetMe.ts}` added via `npm run codegen`, mirroring `GetSettings`'s shape exactly. `src/stores/AuthStore.ts` added (`id`/`email`/`username`, `setMe`/`clear`), wired into `RootStore`. `AppShell.tsx` now reads `store.auth`, falling back to the email's local part when `username` is null (the Google-account gap the implementation-plan index's §4 contract calls out), and is wrapped in `observer`. `lint`/`build`/`test` (44 passed) all clean. |
| T-1.12 | 04.1-manual-signup-and-signin | `RequireAuth`, `router.tsx` | done | — | `frontend/src/app/RequireAuth.tsx` (new): checks `supabaseClient.auth.getSession()` on mount, subscribes to `onAuthStateChange` for live sign-out, triggers `GetMe` once authenticated, redirects to `/sign-in` when not. `router.tsx` restructured: `/sign-up`, `/verify-email`, `/sign-in` as top-level routes outside the guard; the existing `AppShell` route tree now sits inside `RequireAuth`. Satisfies index §8's T-X.1/T-X.2 (redirect when signed out, normal render once a session exists) structurally; not yet exercised in a real browser — see the live-verification task. |
| T-1.13 (frontend half) | 04.1-manual-signup-and-signin | Remove the dev-only `__supabaseClient` window exposure | done | — | Deleted from `frontend/src/api/lib/supabaseClient.ts`, now that real sign-in screens replace the console workaround it existed for. |
| T-1.13 (dashboard half) | 04.1-manual-signup-and-signin | Supabase project settings: Resend SMTP, OTP email template | done | — | User confirmed 2026-09-19: custom SMTP on with Resend credentials, "Confirm signup" template swapped from `{{ .ConfirmationURL }}` to `{{ .Token }}`. Not independently re-verified against a live send in this session |
| T-1.4 (amended) | 04.1-manual-signup-and-signin | Register the Before User Created hook | done | — | User confirmed 2026-09-19: the dashboard shows only Send SMS, Send Email, Custom Access Token, Before User Created. Password Verification Attempt is Teams/Enterprise only (confirmed against Supabase's own docs); it cannot be registered on this plan. See T-1.14 |
| T-1.14 (new) | 04.1-manual-signup-and-signin | FR-17's lockout moved into the app: `identity.SignInInteractor`, `SqlAuthAttemptRepository`, `SupabaseAuthService`, `signIn` GraphQL mutation, `SignInController` rewired to call it | done | — | Full description in the Deviations row below. `pytest tests/unit` (107 passed), `mypy --strict` (whole `app/`), `ruff` (whole `app/` and `tests/`) all clean; `npm run test` (65 passed), `lint`, `build` all clean. `frontend/schema.graphql` regenerated. Integration tests not run — no database reachable from this environment, same constraint noted throughout this feature; not run against a live browser either |
| T-3.2 | 04.3-google-sign-in | Confirm Google is enabled in Supabase Auth settings | done | — | Cannot be toggled from this environment (no Supabase Management API token in `backend/.env`). Confirmed empirically instead, via T-3.5: the OAuth redirect reached a real `accounts.google.com` consent screen rather than a Supabase "provider not enabled" error, so the provider is on. |
| T-3.3 | 04.3-google-sign-in | Migration `0010_google_username_fallback.py` | done | — | `backend/app/migrations/versions/0010_google_username_fallback.py`. `CREATE OR REPLACE FUNCTION handle_new_user()`, identical to 0007's version except a null `raw_user_meta_data->>'username'` now generates `'user_' \|\| substr(NEW.id::text, 1, 8)` via `COALESCE` instead of inserting null. `downgrade()` restores 0007's original body verbatim. Applied to the dev database (`alembic upgrade head`). Verified live at the SQL level: a rolled-back transaction inserted two `auth.users` rows directly (one with `raw_user_meta_data: {"username": "manual_test_user"}`, one with `{}`) and read back `profiles` — manual path kept `manual_test_user` unchanged, empty-metadata path generated `user_8bbb7638` (first 8 chars of that row's UUID). Transaction rolled back, confirmed 0 rows remain. A real `supabase.auth.signUp()`-driven verification was attempted first and blocked by the same email-send rate limit the 2026-09-15 deviation rows above already flag as exhausted this session; the SQL-level test above exercises the same trigger and function this migration changes, independent of that unrelated GoTrue throttle. |
| T-3.4 | 04.3-google-sign-in | Wire the Google button on `SignUpController` and `SignInController` | done | — | Both files' `handleGoogleSignIn` no-op replaced with `await supabaseClient.auth.signInWithOAuth({ provider: "google", options: { redirectTo: window.location.origin + "/" } })`. Left as `onClick={handleGoogleSignIn}` unchanged (not wrapped in `() => void ...`): both files already pass an async handler directly to an `on*` prop this way (`onSubmit={handleSubmit}` in both), so this matches the codebase's own established convention; `oxlint` (this project's configured linter, not `typescript-eslint`) raises nothing on either form. `lint`/`build`/`test` (44 passed) all clean. |
| T-3.5 | 04.3-google-sign-in | Confirm automatic identity linking (FR-20, AD-6) against the real project | partially confirmed | — | The user completed the consent flow live with two different real Google accounts (2026-09-15), landing signed in both times with the account chooser shown (T-3.7) and the profile picture rendered correctly (T-3.8). This confirms the OAuth round trip end to end. Still unconfirmed: neither login is known to match an existing manual signup's email, so FR-20's auto-link-into-a-verified-manual-account path specifically has not been exercised. Needs one more pass: sign up manually with an email, verify it, then sign in with Google using that same email, and confirm one account results. |
| T-4.1 | none — no approved sub-plan covers sign-out | `Popover` primitive | done | — | `frontend/src/design-system/components/Popover.tsx` (new). Generic trigger+panel: `trigger` and `children` are render props (`children` may be a function receiving `close`), internal open state, click-outside via `pointerdown`, Escape-to-close, `placement` variant (`bottom-start`/`bottom-end`/`top-start`/`top-end`) via `cva`. No domain knowledge of what it renders. Styled with `bg-card`/`border-border-strong`/`shadow-lg`, tier-2 tokens only. |
| T-4.2 | none — no approved sub-plan covers sign-out | Sign-out: `AppShell` account-row popover wiring | done | — | `frontend/src/app/AppShell.tsx`: the rail-foot account row is now a real `<button>` (was a `<div>`) wrapped in `Popover`, `aria-haspopup="menu"`/`aria-expanded`. Panel has two items: `Settings` (`Link` to `/settings`, closes on click) and `Sign out` (`await supabaseClient.auth.signOut()`, closes first). No manual store clear or navigation — `RequireAuth.tsx`'s existing `onAuthStateChange` listener does both on session going null. Item styles added to `frontend/src/app/styles.ts` (`accountMenuItemStyles`, `hover:bg-background`, matching `RecordsController`'s row-hover convention). `lint`/`build`/`test` (44 passed) all clean. Live-verified: signed in via Google (real session, already authenticated in the browser used for this check — no consent screen needed), clicked the account row, popover opened with both items, clicked Sign out, landed on `/sign-in`. Screenshot taken before the click. |
| T-4.3 | none — no approved sub-plan covers sign-out | Skeleton loader for the account row while `GetMe` is in flight | done | — | Asked for mid-task. `RequireAuth` renders `AppShell` as soon as the session check passes, before `GetMe` resolves, so `store.auth.id` (and name/email/avatar) is briefly `null`. `AppShell.tsx` now derives `isProfileLoading = id === null` and swaps the avatar and the two text lines for pulsing placeholders (`avatarSkeletonStyles`, `accountNameSkeletonStyles`, `accountEmailSkeletonStyles` in `styles.ts`, all `bg-border-strong` + `animate-pulse`, sized to match the real content so nothing shifts on resolve). `lint`/`build`/`test` (44 passed) clean. Live-verified: reloaded `/` and caught the skeleton frame on the rail-foot row before it resolved to `user_b02e40f7` / `sai369space@gmail.com`. |
| T-3.7 | 04.3-google-sign-in | Force Google's account chooser (`prompt: select_account`) | done | — | `SignUpController.tsx` and `SignInController.tsx`'s `signInWithOAuth` calls both gained `options.queryParams: { prompt: "select_account" }`. Without it, an existing browser Google session with prior consent (exactly the state this session's own earlier live-testing left the browser in, per T-3.5/T-4.2's notes) silently re-authenticates with no chooser, which read as "logs in directly" to the user. `lint`/`build` clean; not re-verified live in-browser after this specific change (the earlier T-3.5/T-4.2 sessions already demonstrated the silent-reauth behavior this fixes). |
| T-3.8 | 04.3-google-sign-in | Migration `0012_backfill_avatar_url.py` | done | — | Bug reported live by the user: their Google account's avatar wasn't rendering. Root cause confirmed by querying the dev database directly: `sai369space@gmail.com`'s `profiles` row was created 2026-09-14, before `0011_profile_avatar_url` existed, so `handle_new_user()` never had a chance to set it — `handle_new_user()` only fires on `auth.users` INSERT, never a repeat sign-in, so the column stayed null forever even though `auth.users.raw_user_meta_data->>'avatar_url'` (kept current by Supabase on every OAuth sign-in) already had the real URL. One-time data backfill: `UPDATE profiles ... FROM auth.users WHERE avatar_url IS NULL`, applied to the dev database. Verified: the affected row now reads the correct `lh3.googleusercontent.com` URL. `downgrade()` is a deliberate no-op — a backfilled row is indistinguishable from one that already had a value, so there is nothing safe to null back out. `ruff` clean. Not yet re-checked in the browser (needs a page reload to refetch `me`, since `RequireAuth` only triggers `GetMe` once per session, not on every render) — worth confirming after this lands. |
| T-3.6 | 04.3-google-sign-in | `avatar_url` on `profiles`, `Me`, `AuthStore`, `AppShell` (FR-21) | done | — | Migration `0011_profile_avatar_url.py`: `ALTER TABLE profiles ADD COLUMN avatar_url text`; `handle_new_user()` replaced again, `COALESCE(raw_user_meta_data->>'avatar_url', raw_user_meta_data->>'picture')`. `ProfileDTO`/`Profile` model/`SqlProfileRepository`/`GetProfileInteractor`/`Me` type all carry it through; `GetProfileInteractor`'s no-row fallback sets `avatar_url=None` too. `frontend/schema.graphql` regenerated, `GetMe` operation/codegen/`responseHandler`/`AuthStore` updated. `AppShell` renders `<img>` when `avatarUrl` is set, the letter avatar otherwise (manual accounts, or a Google account with no picture). Backend: `ruff`/`mypy` (same 3 pre-existing, unrelated errors) / `pytest -m "not live"` (146 passed) all clean; applied to the dev database. One pre-existing integration test broke and was fixed as part of this: `test_me_returns_a_profile_with_no_username` asserted a null username for a no-metadata signup, which was true under 0007 but became false the moment 0010 (T-3.3, the username fallback) landed — renamed to `test_me_generates_a_username_when_signup_metadata_carried_none` and now asserts the generated value. Frontend: `lint`/`build`/`test` (44 passed) all clean. |
| T-5.1 | none — no approved sub-plan; this is epic 001's Settings/theme toggle (design Q13, reopened and reversed 2026-09-15), built here since this dev log is the one open this session | Theme preference: `src/utils/themePreference.ts`, `index.html` pre-paint script, `tokens.css` restructured for a `[data-theme]` override | done | — | `themePreference.ts` (new): `ThemePreferenceType = "SYSTEM" \| "LIGHT" \| "DARK"`, localStorage-backed (`slashit:theme-preference`), not a MobX store (repo-rules.md §7 reserves stores for server state). `index.html` gained a synchronous inline `<script>` in `<head>`, before any module script, reading the same key and setting `data-theme` on `<html>` before first paint — mirrors the module's key/values by comment, not by import, since it must run with zero framework code. `tokens.css`: header comment rewritten, the `prefers-color-scheme` block rescoped to `:root:not([data-theme="light"])` so an explicit light override still wins over a dark OS, and a new `:root[data-theme="dark"]` block added with byte-identical values (Tailwind v4 `@theme` can't share a block across an `@media` rule and a plain selector, so it's a deliberate duplicate, called out in both the header comment and each block's own comment). |
| T-5.2 | none — same as T-5.1 | Theme control on `SettingsController`, under a new "Appearance" section | done | — | Three `Button`-based options (System/Light/Dark), `aria-pressed` on the selected one, styled with an `accent`/`accent-wash` "on" state matching this file's existing note-box tokens (`Styles.themeOptionOnStyles`); state read from `getThemePreference()` on mount, written via `setThemePreference()` on click. Matches the Timezone section's label/select/note structure above it. `lint`/`build`/`test` (44 passed) clean. Live-verified at `/settings`, signed in as `user_e4ad7d61`: OS reports dark (`matchMedia('(prefers-color-scheme: dark)').matches === true`), System rendered dark correctly. Clicking Light flipped the whole page to the light palette immediately, overriding the dark OS setting; reloading with Light stored re-rendered light on the very first frame, and `data-theme` read `"light"` right after navigation, with the inline script confirmed (via a raw `curl`) to sit before both Vite's module scripts and `main.tsx` in the served HTML, so it runs synchronously before any stylesheet resolves — no flash. Dark likewise overrode the OS. Reset to System afterward; `localStorage` confirmed back to `"SYSTEM"`. |

| T-2.1 | 04.2-password-reset | `ForgotPasswordController` | done | — | `frontend/src/features/auth/controllers/ForgotPasswordController/{ForgotPasswordController.tsx,styles.ts}`. All five states from `02-design.md` §4 (empty, loading, error, rate-limited, success). Calls `supabaseClient.auth.resetPasswordForEmail` directly, no store or GraphQL operation, matching slice 1's screens. Client-side validation is format-only and never reports whether an account exists (FR-12's enumeration posture, the same reasoning as FR-11) — the success card renders on every well-formed address. Rate-limit detection matches `error.code` against `over_email_send_rate_limit`/`over_request_rate_limit` first with HTTP 429 as a fallback, the shape slice 1's own live correction on 2026-09-15 established; a bare 429 check alone was already proven wrong there. `lint`/`build`/`test` all clean. |
| T-2.2 | 04.2-password-reset | `ResetPasswordController`, both steps | done | — | `frontend/src/features/auth/controllers/ResetPasswordController/{ResetPasswordController.tsx,styles.ts}`. One controller, two steps (`StepType` = `CODE` \| `PASSWORD`) per the canvas's single `ResetPassword` artboard with a `step` tweak. Code step reuses slice 1's `OtpInput` unchanged and auto-submits `verifyOtp({ type: "recovery" })` on the 6th digit, with an explicit `Continue` button as well; resend calls `resetPasswordForEmail` again behind the same 30s client-side cooldown `VerifyEmailController` uses, with `aria-live="polite"` on the row (this closes the Q2 item deferred out of T-1.7). Password step calls `updateUser({ password })`, then signs the recovery session out and redirects to `/sign-in`. Wrong and expired codes deliberately share one message here (NFR-2, design §8), which sidesteps the `otp_expired` ambiguity logged against `VerifyEmailController` on 2026-09-15 rather than repeating it. `lint`/`build`/`test` all clean. |
| T-2.3 | 04.2-password-reset | Wire `router.tsx` and the Sign in "Forgot password?" link | done | — | `frontend/src/app/router.tsx` gained `/forgot-password` and `/reset-password` as top-level routes outside `RequireAuth`, beside the other three auth routes. `SignInController`'s "Forgot password?" link already pointed at `/forgot-password` (written during T-1.10 against a route that did not yet exist, so it was dead); it now resolves. No change to `SignInController.tsx` was needed — verified in a browser by clicking the link through from `/sign-in`. |
| T-2.4 | 04.2-password-reset | Manual check of Supabase's reset-email rate limit against the drafted copy | not started | — | Needs a real Supabase project; no credentials are reachable from this environment (`frontend/.env` does not exist here). The rate-limited card itself is verified rendering-wise against an injected throttle response (see below) — what is unverified is that a real `resetPasswordForEmail` throttle returns one of the two matched `error.code` values. Carried to Deferred. |
| T-2.5 | 04.2-password-reset (design §7, Focus order) | `autoFocus` on `OtpInput`, used by both code screens | done | — | Found while re-reading the slice against `02-design.md` §7: "On entering Verify email or Reset password's code step, focus starts in the first code box" was never implemented, on either screen. `OtpInput` gained an optional `autoFocus` prop (default `false`, so nothing else changes) that focuses box 1 on mount only — deliberately not on every `disabled` flip, which would yank focus mid-retype after a failed verify. Both `ResetPasswordController` and `VerifyEmailController` now pass it. Verified in a browser: `document.activeElement` reads `Digit 1 of 6` on load of `/reset-password` and `/verify-email`, and the whole reset flow now completes keyboard-only from page load with no click. Touching slice 1's shipped `VerifyEmailController` was deliberate — see the deviation row. |

## Deviations from the plan
| Date | Planned | Actual | Why | Approved by |
|---|---|---|---|---|
| 2026-09-14 | Canvas dot-grid uses raw `#ddd2bf` | Reused the existing `--color-border-strong` semantic token (`#d6ccbc` light / dark-mode equivalent) instead of adding a new token | `#ddd2bf` is within a few RGB units of `--color-border-strong`, imperceptible at 1.2px dots on a 26px grid; repo rules forbid raw hex in a feature and require tier-2 tokens only, so the close existing token was reused rather than adding a near-duplicate | claude, per task instructions (add a token only if genuinely no existing one is close enough) |
| 2026-09-14 | `jobs.py` holds the query and calls it directly | Added `interactors/purge_unverified_accounts.py` and `repositories/auth_account_repository.py` (plus an `AuthAccountRepository` Protocol) instead | 04.1's file-by-file plan lists only `jobs.py` for T-1.6, but repo-rules.md §14 requires a task to "resolve its collaborators from `deps.py` and call one interactor"; a raw DELETE inline in the task body would be the same anti-pattern as business logic in a resolver | claude, applying repo-rules.md over the plan's file list, which is not exhaustive of internal layering |
| 2026-09-14 | `Me.email` sourced from `ProfileDTO` | Added `Context.email: str \| None` and `core/auth.decode_email_claim()`, left `verify_token()`'s signature and return type unchanged | The plan's own note flagged `Context` might need extending; changing `verify_token` to return an object instead of a bare `UUID` would have broken every other integration test's token-building helper (none set an `email` claim, all assert `verify_token(...) == user_id` in `test_auth.py`), so email is read from an already-verified token by a second, explicitly-unverified decode instead | claude |
| 2026-09-14 | `hook_password_verification_attempt`, `hook_before_user_created` payload/response shapes verified against a live Supabase project | Written to Supabase's documented Auth Hook conventions but not checked against a real hook invocation; flagged inline in `0009_auth_hooks.py`'s docstring and at each specific line | No live Supabase project with hooks configured is reachable from this environment; T-1.4 (registering the hooks) is explicitly out of scope and is where this must be checked before going live | claude, per task instructions to flag rather than guess silently |
| 2026-09-14 | `auth.users`' confirmation column name confirmed against a live schema | Used `email_confirmed_at`, Supabase's documented column, without a live schema browse to confirm (no reference to it existed elsewhere in this codebase) | Task instructions allowed a stated best-documented assumption when unverifiable; `email_confirmed_at` is the standard, non-deprecated column (`confirmed_at` is a deprecated generated column) | claude |
| 2026-09-14 | — | `identity/jobs.py` opens its own SQLAlchemy engine/session factory (lazily, module-level), separate from the pool `main.py` builds for GraphQL requests | A Procrastinate task has no per-request `Context` to draw a session from, and a worker process (`procrastinate worker`) normally runs as a separate OS process from the web app anyway, so a second pool is the right default; if the two are ever run in one process this doubles pooled connections, worth revisiting then | claude |
| 2026-09-14 | Running a `procrastinate worker` process | Not set up. `core/jobs.py` and `main.py`'s lifespan only build the `App`, register the task, and open/close its connector | Deploy/process topology (a separate worker container, `procrastinate worker` in the entrypoint, etc.) is an infra decision with no precedent in this repo yet and out of scope for T-1.6's acceptance check ("T-1.6 passes") | claude |
| 2026-09-14 | — | `tests/integration/test_graphql_auth.py`'s `ME_QUERY` changed from `{ me }` to `{ me { id } }` | The placeholder root `Query.me: str` this test exercised is deleted (04.1's own instruction); the real `me` returns an object type, and a bare `{ me }` now fails GraphQL validation before any resolver or permission class runs, which would have silently broken what T-2.5 actually tests (that `IsAuthenticated` closes the field) | claude |
| 2026-09-14 | `SignUpController`'s username-taken and email-taken errors matched against Supabase's exact `signUp` error text, checked live | Matched by best-documented guess instead: email-taken on `error.code === "user_already_exists"` or `message.includes("already registered")`; username-taken (the `handle_new_user()` trigger's unique-username violation) on `message.includes("database error saving new user")`, GoTrue's documented generic text for a failed post-insert trigger. Rate-limited matched on `error.status === 429` or `message.includes("too many attempts")` against the hook's own literal text in `0009_auth_hooks.py` | No live Supabase project with the T-1.3/T-1.4 hooks and T-1.1 trigger registered is reachable from this environment, same constraint noted against `0009_auth_hooks.py` itself. Flagged in code comments at `classifySignUpError` in `SignUpController.tsx` | claude, per task instructions to flag rather than guess silently |
| 2026-09-15 | Follow-up: RATE_LIMITED match verified live | **Wrong as originally written, now fixed.** Signing up repeatedly against the real project triggers Supabase's own built-in email-send throttle. Its `error.code` is `over_email_send_rate_limit` (and `over_request_rate_limit` for the general one), confirmed against `@supabase/auth-js/src/lib/error-codes.ts` directly — not `"too many attempts"`, which is only `hook_before_user_created`'s literal text and still unverified (T-1.4). Worse, the HTTP status was not reliably 429 either: the very first live signup returned **400** for this same condition, which the old `error.status === 429` check would miss entirely, silently falling through to the generic "Something went wrong" state. Fixed `classifySignUpError` in `SignUpController.tsx` to match `error.code` against `over_email_send_rate_limit`/`over_request_rate_limit` first, keeping status 429 and the hook's message as fallbacks. Re-verified live after the fix: the "Too many attempts" screen now renders correctly. EMAIL_TAKEN (`user_already_exists`) is a real documented code per the same source file, giving it more confidence than a guess, but duplicate-email and taken-username submissions could not be exercised live this session — see the note below. | claude, live verification per this task |
| 2026-09-15 | EMAIL_TAKEN / USERNAME_TAKEN live verification | **Blocked, not completed.** The real Supabase project's built-in email-send rate limit (see row above) was exhausted after ~5 signup attempts made while establishing the account needed for step 2 of the manual verification pass, and did not clear within the session's wait window. A successful signup (needed as the basis for the duplicate-email and taken-username tests) could not be obtained. `classifySignUpError`'s `EMAIL_TAKEN`/`USERNAME_TAKEN` branches are therefore still unverified against a real trigger rejection — re-run this verification once the project's email quota resets, or once Resend SMTP (T-1.13) is configured and the built-in throttle no longer applies. | claude |
| 2026-09-14 | Google button styled with only tier-2 tokens (repo-rules.md §13 rule 6) | Button surface uses `bg-card`/`border-border-strong` (token-exact match to the canvas's white/`--line2`), but its text uses `text-foreground` in place of the canvas's Google-specific `#3c4043`, and the four-color "G" mark's SVG `fill`s are left as the literal hex Google's own brand mark requires | Task instructions required copying the mark's SVG paths verbatim rather than approximating them; `#3c4043` has no close existing token (unlike the dot-grid precedent above) so the nearest semantic token was used for the text instead of adding a near-duplicate one | claude |
| 2026-09-14 | Exact copy for a `signUp` network failure or an unrecognised error | No copy specified anywhere in `02-design.md` or `04.1-manual-signup-and-signin.md` beyond "a generic retry-safe error"; used "Something went wrong. Try again." in the same red banner as the named error states | 04.1 §6 describes the state but not its wording; marked `> Assumption:` inline in `SignUpController.tsx` | claude |
| 2026-09-14 | `SignInController`'s lockout state (FR-17) matched against `hook_password_verification_attempt`'s exact rejection shape, checked live | Matched by best-documented guess: `error.message.includes("Too many attempts")` against the hook's own literal message text in `0009_auth_hooks.py`, since `@supabase/auth-js`'s `ErrorCode` union has no dedicated code for a rejected Auth Hook (checked `error-codes.ts` directly: nothing password-verification- or hook-rejection-shaped exists there). A network failure or any other unclassified `signInWithPassword` error also falls into the plain error state by the same fallthrough, per 04.1 §6's "generic retry-safe error" row | Same constraint as `SignUpController`'s and `VerifyEmailController`'s rate-limit matches: no live Supabase project with T-1.3's hook and T-1.4's registration reachable from this environment. This is the one distinction task T-1.10 asked to be flagged explicitly if not solid — it is not solid, unlike the unverified-account distinction (`email_not_confirmed`), which is a real SDK error code | claude, per task instructions to flag rather than guess silently |
| 2026-09-15 | Follow-up: `SignInController` live verification | **Partially confirmed.** Wrong-password/unregistered-email against the real project renders "That email or password is not right." exactly, confirming FR-11's byte-identical requirement holds against real `invalid_credentials`/similar errors, not just by construction. The lockout match itself (`"Too many attempts"`) is still unverified: T-1.4 remains unregistered in this project (confirmed — repeated wrong-password attempts never locked, just kept returning the generic error, as expected), so no real hook rejection was ever produced to test the string against. Left unchanged, no live signal to fix it with. | claude, live verification per this task |

| 2026-09-14 | — | `SignInController`, `SignUpController`, `VerifyEmailController`'s local string-union state types (`ViewState`, `Phase`, `SignUpErrorKind`, `Stage`, `ErrorKind`) renamed to a `Type` suffix with uppercase members (`ViewStateType`, `PhaseType`, `SignUpErrorKindType`, `StageType`, `ErrorKindType`) | User added a new `frontend/rules/repo-rules.md` §13 rule (local string unions take a `Type` suffix, uppercase members) mid-build, after T-1.8 to T-1.10 had already shipped with lowercase values. Retrofitted immediately rather than left inconsistent with new code | user, rule added directly; retrofit done by claude |

| 2026-09-15 | — | Built a sign-out affordance (account-row popover with Settings/Sign out, T-4.1/T-4.2) with no FR in `01-prd.md` and no task in any approved sub-plan | Asked for directly, out of the normal gate sequence | user, per task instructions; a PRD/epic amendment for a sign-out requirement is still needed on the docs side — not made here, that edit belongs to the user |
| 2026-09-17 | The password step has a "Confirm new password" field, per the canvas, but no copy for a mismatch | Invented "Both passwords must match." and "Enter a new password." for the empty case | The canvas draws the password step in its resting state only, and `02-design.md` §8's copy table has no row for either. Marked `> Assumption:` in place in `ResetPasswordController.tsx` | claude, per root CLAUDE.md rule 3 |
| 2026-09-17 | — | On `updateUser` success the controller calls `signOut()` before showing the confirmation | `verifyOtp({ type: "recovery" })` opens a real session — that is what `updateUser` needs — so without this the user would be silently signed in and `RequireAuth` would let them into the app. `02-design.md` §3's flow diagram ends `RP2 → SI`, and §4's success copy is "Sign in with your new password.", so the session is closed to make that copy true rather than left open | claude |
| 2026-09-17 | A rejected new password shows the generic retry error | Supabase's own message is surfaced verbatim for `error.code === "weak_password"`, generic for everything else | Supabase's text names the actual rule ("Password should be at least N characters"), which the project owner sets in the dashboard; duplicating it as a Slashit string would go stale the moment that setting changes. No design copy exists for this case either way. Marked `> Assumption:` in place | claude |
| 2026-09-17 | The canvas's field-level line "Missing the domain part" under the email input | Rendered for exactly the case the canvas draws it for (an address with an `@` but no dot in the domain), omitted otherwise | It is the only field-level message the canvas specifies, and it is specific to `jordan@example`. Inventing a second one for other malformed shapes would be copy nobody approved; the banner ("Enter a valid email address.") covers those | claude |
| 2026-09-17 | Test plan T-2.1 is listed as `e2e` | Delivered as component tests plus a scripted real-browser pass, not an e2e suite | This repo has no e2e harness (no Playwright or Cypress in `frontend/package.json`; `repo-rules.md` §14's table lists Vitest, Testing Library and Storybook only). Standing one up is a tooling decision beyond this slice. The browser pass covered the same path — see the row below | claude |
| 2026-09-17 | Live verification against the real Supabase project, as slices 1 and 3 had | Verified in a real headless Chromium against the dev server with Supabase's HTTP responses intercepted, not against the live project | `frontend/.env` does not exist in this environment and no Supabase credentials are reachable from it, so a real `resetPasswordForEmail` could not be sent. What this pass does prove: every screen renders, the `/sign-in` → `/forgot-password` → `/reset-password` navigation resolves, the 6-digit auto-submit fires `verifyOtp` with `type: "recovery"` and the right email, both steps advance, the mismatch guard holds, and the success path lands on `/sign-in` — with no page errors, in light and dark. What it does not prove: that a real Supabase throttle or a real rejected code returns the `error.code` values matched here. That is T-2.4, deferred | claude |
| 2026-09-17 | Slice 2 touches only the four files in `04.2-password-reset.md` §4 | Also changed `features/auth/components/OtpInput.tsx` and slice 1's `VerifyEmailController.tsx`, to add the focus-order behaviour (T-2.5) | `02-design.md` §7 requires it for both code screens and neither had it. The change to `OtpInput` is additive (a prop defaulting to `false`), so slice 1's screen would have been unaffected had it been left alone — but leaving an approved accessibility requirement unimplemented on one of the two screens it names, while fixing it on the other, is an arbitrary split. Fixed in both and logged here rather than done silently | claude |

| 2026-09-19 | AD-7: FR-17 enforced by the Password Verification Attempt Auth Hook | The hook is Teams/Enterprise only — confirmed both against Supabase's own docs and against the live project's dashboard, which offers only Send SMS, Send Email, Custom Access Token, Before User Created. `hook_password_verification_attempt` (migration `0009`) can never be registered on this plan, and is dropped (migration `0015`). FR-17 moves into the app: `identity.SignInInteractor` orchestrates, `SqlAuthAttemptRepository` re-implements the dropped function's counting logic in Python keyed by email rather than user id (the app knows the email before any user lookup exists; the old hook only knew a user id because Supabase had already resolved one), and `SupabaseAuthService` calls Supabase's Auth REST API (`/auth/v1/token?grant_type=password`) directly with the publishable key, the same endpoint `supabase-js`'s `signInWithPassword` calls over HTTP. `SignInController` now calls a new `signIn` GraphQL mutation instead of `supabaseClient.auth.signInWithPassword` directly — the one exception to AD-2's "no GraphQL proxy for auth" rule, chosen over having the frontend self-report success/failure to the backend, which a malicious client could always skip. This also retires two guessed signals `SignInController`'s own dev log rows had flagged as unverified: the `"Too many attempts"` string match for lockout, and the ad hoc detection is replaced by the mutation's typed `AccountLocked`/`InvalidCredentials`/`AccountNotVerified`/`AuthProviderUnavailable` union. One new finding surfaced while building this: Supabase's REST error body carries its string error code under `error_code` (API versions before 2024-01-01) or `code` (on and after) — neither checked against this project's actual API version, so `SupabaseAuthService._error_code_from` reads both defensively; flagged in the module's own docstring | User confirmed the hook is missing from the dashboard, then chose the backend-proxy shape (over the frontend-reports-outcome alternative) when asked directly | user, backend-proxy shape; claude, the email-not-confirmed handling and the defensive error-code parsing, found while implementing |

## Deferred
| Item | Why deferred | Where it goes next |
|---|---|---|
| Q2 (`02-design.md` §9): exact `aria-live` wording for the OTP resend cooldown | Out of scope for T-1.7 (`OtpInput` has no resend control; that lives in the screen controllers) | `VerifyEmailController` / `ResetPasswordController` (T-1.9, sub-plan 04.2) |
| Storybook story for `AuthCard` / `OtpInput` | `repo-rules.md` §11.4/§14 calls for one per primitive, but no Storybook tooling is installed in `frontend/` yet (not in `package.json`, `src/design-system/stories/` is empty) | Whoever sets up Storybook for the design system |
| T-1.4: register `hook_before_user_created` in a Supabase project's Auth Hooks config. **Password Verification Attempt is no longer part of this item** — done 2026-09-19, see the Task status and Deviations rows above | Needs a real Supabase dashboard/`config.toml`, out of scope for this agent | Done. Still open: verify `hook_before_user_created`'s payload/response shape flagged in `0009_auth_hooks.py` against a real signup rejection, and the `signUp` error strings `SignUpController.tsx`'s `classifySignUpError` matches against |
| Running a `procrastinate worker` process | Deploy/process topology decision, no precedent in this repo | Whoever owns deploy config for this feature |
| T-2.4: confirm a real `resetPasswordForEmail` throttle returns `over_email_send_rate_limit` or `over_request_rate_limit`, and that the drafted "Too many requests" copy fits it | Needs a real Supabase project; no credentials reachable from this environment | Whoever has project access. Same class as slice 1's rate-limit strings, and the same fix applies: once Resend SMTP (T-1.13) is configured the built-in email throttle stops being the thing under test |
| Slice 2 end to end against the real project: request a reset, receive the mailed code, set a new password, sign in with it (test-plan case T-2.1) | Same constraint — needs a real project and a deliverable mailbox | Whoever has project access, alongside T-1.13's SMTP work |

## Incidents and defects
| Date | What broke | Cause | Fix |
|---|---|---|---|
| 2026-09-15 | `VerifyEmailController`'s wrong-code state can never render; every invalid code shows "This code expired" instead | T-1.9's row above claims the wrong-vs-expired split is "distinguished cleanly via `error.code === 'otp_expired'`... no ambiguity to log". That claim does not hold: `otp_expired` is the *only* code `@supabase/auth-js`'s `ErrorCode` union has for an invalid OTP (checked `error-codes.ts` directly — no separate "wrong"/"invalid" code exists). Confirmed live: entering 6 wrong digits against an email with no real pending code returned `otp_expired`, rendering EXPIRED. Supabase's API gives no signal to tell "mistyped" from "genuinely past its TTL" apart, so `ErrorKindType`'s `WRONG` branch is currently dead code. | Not fixed. There is no error-matching string to correct here — the underlying premise (that the API distinguishes the two) is false, and choosing a new heuristic (e.g., time-since-send) is a design decision outside this task's authority to invent. Needs the design owner or an engineer with a live project to decide: collapse to one state, or find another signal. |
| 2026-09-15 | `me` GraphQL query threw an unhandled server error instead of returning `username: null` for a profile-less account | `GetProfileInteractor.get_profile` (`backend/app/domains/identity/interactors/get_profile.py:20`) raises `ProfileMissingError` when no `profiles` row exists, and `IdentityQueries.me` (`backend/app/domains/identity/graphql/queries.py:36-43`) never catches it — it reaches the client as a raw GraphQL error ("No profile row for user ..."), not the typed union `repo-rules.md` §8 requires. Reproduced live against a pre-existing project account with no `profiles` row. Contradicts T-1.5's own acceptance check ("`me` returns `username: null` for a user with no profile row"). | **Fixed 2026-09-15.** `get_profile` now returns `ProfileDTO(user_id=user_id, username=None, created_at=None)` instead of raising when the repository finds no row, matching the approved acceptance check over the agent's own (thoughtfully reasoned, but unapproved) choice to treat it as a hard defect. `ProfileDTO.created_at` became `datetime \| None` (not exposed through `Me`, so nothing downstream sees it). `identity/errors.py` and `ProfileMissingError` deleted — nothing raises it any more. `tests/unit/test_get_profile_interactor.py` updated: the old `pytest.raises(ProfileMissingError)` case is now `test_no_row_at_all_returns_a_null_username_not_an_error`. `ruff`, `mypy` (same 3 pre-existing, unrelated errors as before), and `pytest -m "not live"` all clean. | claude |
| 2026-09-15 | T-4.2's account-row footer rendered right below the nav instead of pinned to the rail's bottom (user caught this live, mid-verification) | `railFootStyles`' `mt-auto` was on the trigger `<button>`, but `Popover` wraps every trigger in its own plain `<div>` — the button's real parent. `margin-top: auto` only resolves against a flex/grid container; the button's parent is neither, so the margin computed to 0 and the row sat directly under `Settings` instead of at the bottom of `railStyles`' flex column. | Added `Popover`'s `containerClassName` prop, applied to its own wrapper div (the actual flex item) rather than the caller-supplied trigger. `AppShell` now passes `containerClassName="mt-auto"` to `Popover`; `mt-auto` removed from `railFootStyles`. Re-verified live: footer pinned to the bottom again, popover opens clear of the nav items. `lint`/`build`/`test` (44 passed) re-run clean. |
| 2026-09-15 | Google-account avatar not rendering; user reported it twice after the T-3.8 backfill migration first landed | `0012_backfill_avatar_url.py` fixed the database (confirmed by direct query), but the user's browser session had not refetched `me` since — `RequireAuth` only triggers `GetMe` once per session, not on every render, so a signed-in tab kept the stale `avatarUrl: null` it had already fetched | **Confirmed fixed.** After a reload against the backfilled data the user confirmed live: "profile image is working fine." No further code change was needed past T-3.8; the two reports were the same stale-fetch window, not a second defect. |

## Next up
| Priority | Item | Status |
|---|---|---|
| This feature, manual | T-2.4 and slice 2's end-to-end pass against the real project (request a reset, receive the code, set a new password, sign in with it) — T-1.13's SMTP is now configured, so this is unblocked | Needs the user's project access |
| This feature, manual | T-1.10 live verification: the new `signIn` mutation's lockout, unverified-account and provider-unavailable states against the real project | Needs the user's project access |
| This feature, manual | T-3.5: one more Google sign-in, using an email that already has a verified manual account, to confirm FR-20's auto-link | Needs the user |
| This feature, design decision | `VerifyEmailController` wrong-vs-expired OTP: Supabase gives one code for both; collapse to one state or find another signal | Needs the design owner |
| Next epic (registry priority, both depend on 001 + 002) | 003 Reminders and Notifications, or 004 Persistent Memory | Not started — epic stage |

## Notes for the next feature
- `frontend/src/design-system/stories/` exists as an empty directory but Storybook itself is not wired into the project. `repo-rules.md` assumes it is. Worth flagging before the next feature that needs a catalog.
- Running the backend locally against the real Supabase JWKS endpoint failed with `PyJWKClientConnectionError` / `SSL: CERTIFICATE_VERIFY_FAILED: unable to get local issuer certificate` until `uvicorn` was started with `SSL_CERT_FILE` pointed at the venv's `certifi` bundle (`SSL_CERT_FILE=$(python3 -c "import certifi; print(certifi.where())")`). This is a local machine/Python install gotcha (the `python.org` framework build ships no system CA bundle by default), not an application bug — `backend/.venv` already depends on `certifi`, it just is not wired as the default SSL context. Worth a line in backend setup docs so the next person does not lose time to it.
